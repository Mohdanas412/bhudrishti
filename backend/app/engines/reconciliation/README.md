# Reconciliation Engine — owner M5

Conflict -> Evidence -> Rule -> Recommendation (playbook Section 9).
Action values must come from the enum in `app/api/reconciliation.py` /
`docs/contracts/recommendation.json` — do not invent new action strings here.
